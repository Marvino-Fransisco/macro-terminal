import { cva } from "class-variance-authority";

export const radioVariants = cva(
  [
    "",
  ],
  {
    variants: {
      variant: {
        default: [

        ],
        error: [

        ],
      },
    },
    defaultVariants: {
      variant: "default",
    }
  }
);
